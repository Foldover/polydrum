import { OverlayActionDirective } from './overlay-action.directive';

describe('OverlayActionDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayActionDirective();
    expect(directive).toBeTruthy();
  });
});
